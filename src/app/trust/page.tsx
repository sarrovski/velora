import { Nav } from "@/components/nav";
import { Card, Metric, Shell, StatusBadge } from "@/components/ui";
import { reports, sellers } from "@/lib/data";

export default function TrustPage() {
  return <Shell><Nav /><section className="mt-12"><p className="text-sm font-medium text-cyan-300">Trust center</p><h1 className="mt-3 text-5xl font-black">Marketplace safety and transparency</h1><p className="mt-3 max-w-3xl text-slate-400">Standard makes seller reputation, tool status, reports, appeals, and paid placement visible so users can choose with context.</p></section><section className="mt-8 grid gap-4 md:grid-cols-4"><Metric label="Open reports" value={String(reports.filter(r=>r.status==="Open").length)} /><Metric label="Restricted sellers" value={String(sellers.filter(s=>s.status==="Restricted").length)} /><Metric label="Verified sellers" value={String(sellers.filter(s=>s.status==="Verified").length)} /><Metric label="Paid rank influence" value="0" /></section><section className="mt-8 grid gap-6 lg:grid-cols-[1fr_340px]"><Card className="p-6"><h2 className="text-2xl font-black">Active reports</h2><div className="mt-5 space-y-3">{reports.map(report => <div key={report.id} className="rounded-2xl border border-white/10 bg-slate-950/40 p-4"><div className="flex flex-wrap items-center justify-between gap-3"><div><div className="font-semibold">{report.target}</div><div className="mt-1 text-xs text-slate-500">{report.id} • {report.type}</div></div><div className="flex gap-2"><StatusBadge value={report.status} /><StatusBadge value={report.severity} /></div></div><p className="mt-3 text-sm text-slate-400">{report.note}</p></div>)}</div></Card><aside className="space-y-4"><Card className="p-5"><h3 className="text-xl font-bold">Trust score inputs</h3><div className="mt-4 space-y-3 text-sm text-slate-400"><p>✓ Positive/neutral/negative reviews</p><p>✓ Dispute rate</p><p>✓ Seller response time</p><p>✓ Product status accuracy</p><p>✓ Admin restrictions</p></div></Card><Card className="p-5"><h3 className="text-xl font-bold">User rule</h3><p className="mt-3 text-sm leading-6 text-slate-400">Standard can recommend discovery, but the user should always verify off-platform purchases and avoid risky sellers.</p></Card></aside></section></Shell>;
}
