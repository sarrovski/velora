import { Nav } from "@/components/nav";
import { auditLogs, plans, reports, reviews, sellers, tools } from "@/lib/data";
import { Badge, Card, Metric, Shell, StatusBadge } from "@/components/ui";
import type { ReactNode } from "react";

export default function AdminPage() {
  return <Shell><Nav /><section className="mt-12"><Badge tone="red">Admin control</Badge><h1 className="mt-4 text-5xl font-black">Platform Control Center</h1><p className="mt-3 max-w-3xl text-slate-400">Control sellers, products, review appeals, boosts, risk reports, plan limits, and marketplace trust.</p></section><section className="mt-8 grid gap-4 md:grid-cols-4"><Metric label="Sellers" value={String(sellers.length)} detail="Demo dataset" /><Metric label="Products" value={String(tools.length)} detail="All statuses" /><Metric label="Reports" value={String(reports.length)} detail="Open / monitoring" /><Metric label="Appeals" value={String(reviews.filter(r => r.status === "Appealed").length)} detail="Needs decision" /></section><section className="mt-8 grid gap-6 xl:grid-cols-[1fr_360px]"><div className="space-y-6"><Panel title="Seller control"><div className="space-y-3">{sellers.map(s => <Row key={s.slug} title={s.name} meta={`${s.plan} • ${s.productSlots} products • dispute ${s.disputeRate}`} right={<StatusBadge value={s.status} />} />)}</div></Panel><Panel title="Product moderation"><div className="overflow-x-auto"><table className="w-full min-w-[960px] text-left text-sm"><thead className="border-b border-white/10 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-4 py-3">Product</th><th className="px-4 py-3">Seller</th><th className="px-4 py-3">Platform</th><th className="px-4 py-3">Tool</th><th className="px-4 py-3">Risk</th><th className="px-4 py-3 text-right">Actions</th></tr></thead><tbody>{tools.map(t => <tr key={t.slug} className="border-b border-white/5 last:border-0"><td className="px-4 py-4 font-semibold">{t.name}</td><td className="px-4 py-4 text-slate-400">{t.seller}</td><td className="px-4 py-4"><StatusBadge value={t.status} /></td><td className="px-4 py-4"><StatusBadge value={t.toolStatus} /></td><td className="px-4 py-4"><StatusBadge value={t.risk} /></td><td className="px-4 py-4 text-right"><div className="flex justify-end gap-2"><button className="rounded-lg border border-emerald-400/20 bg-emerald-500/10 px-3 py-2 text-xs font-semibold text-emerald-300">Approve</button><button className="rounded-lg border border-red-400/20 bg-red-500/10 px-3 py-2 text-xs font-semibold text-red-300">Suspend</button></div></td></tr>)}</tbody></table></div></Panel><Panel title="Review appeals"><div className="grid gap-3 md:grid-cols-2">{reviews.map(r => <div key={r.title} className="rounded-2xl border border-white/10 bg-slate-950/40 p-4"><div className="font-semibold">{r.title}</div><p className="mt-2 text-sm text-slate-400">{r.body}</p><div className="mt-3 flex justify-between text-xs text-slate-500"><span>{r.user}</span><StatusBadge value={r.status} /></div></div>)}</div></Panel></div><aside className="space-y-6"><Panel title="Risk reports"><div className="space-y-3">{reports.map(r => <div key={r.id} className="rounded-2xl border border-white/10 bg-slate-950/40 p-4"><div className="flex justify-between"><div className="font-semibold">{r.target}</div><StatusBadge value={r.severity} /></div><p className="mt-2 text-sm text-slate-400">{r.note}</p><div className="mt-2 text-xs text-slate-500">{r.id} • {r.status}</div></div>)}</div></Panel><Panel title="Plan limits"><div className="space-y-3">{plans.map(p => <div key={p.name} className="rounded-2xl border border-white/10 bg-slate-950/40 p-4"><div className="font-bold">{p.name}</div><div className="text-xs text-slate-500">{p.products} products • {p.boosts} boosts • {p.appeals} appeals</div></div>)}</div></Panel><Panel title="Audit log"><div className="space-y-3">{auditLogs.map(log => <div key={log} className="rounded-2xl border border-white/10 bg-slate-950/40 p-4 text-sm text-slate-300">{log}<div className="mt-1 text-xs text-slate-500">Just now • Admin</div></div>)}</div></Panel></aside></section></Shell>;
}
function Panel({ title, children }: { title: string; children: ReactNode }) { return <Card className="overflow-hidden"><div className="border-b border-white/10 p-5"><h2 className="text-xl font-bold">{title}</h2></div><div className="p-5">{children}</div></Card>; }
function Row({ title, meta, right }: { title: string; meta: string; right: ReactNode }) { return <div className="flex items-center justify-between gap-4 rounded-2xl border border-white/10 bg-slate-950/40 p-4"><div><div className="font-semibold">{title}</div><div className="mt-1 text-xs text-slate-500">{meta}</div></div>{right}</div>; }
