import { Nav } from "@/components/nav";
import { Badge, Card, Shell } from "@/components/ui";
import { plans } from "@/lib/data";
import Link from "next/link";

export default function PricingPage() {
  return <Shell><Nav /><section className="mx-auto mt-14 max-w-3xl text-center"><Badge>Seller pricing</Badge><h1 className="mt-4 text-5xl font-black">Simple plans based on product slots.</h1><p className="mt-4 text-slate-400">Subscriptions control catalog capacity. Boosts, pins, and bumps are paid visibility products and never modify organic ranking.</p></section><section className="mt-10 grid gap-5 md:grid-cols-3">{plans.map(plan => <Card key={plan.name} className={`p-6 ${plan.highlighted ? "border-purple-400/40 bg-purple-500/10" : ""}`}>{plan.highlighted && <Badge>Recommended</Badge>}<h2 className="mt-5 text-2xl font-black">{plan.name}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{plan.description}</p><div className="mt-6 flex items-end gap-2"><span className="text-5xl font-black">{plan.price}</span><span className="pb-2 text-sm text-slate-500">/month</span></div><ul className="mt-6 space-y-3 text-sm text-slate-300"><li>✓ {plan.products} products</li><li>✓ {plan.boosts} active boosts</li><li>✓ {plan.appeals} review appeals</li><li>✓ {plan.analytics} analytics</li><li>✓ Seller page</li><li>✓ Product features management</li></ul><Link href="/dashboard" className={`mt-7 inline-flex w-full justify-center rounded-xl px-4 py-3 text-sm font-semibold ${plan.highlighted ? "bg-gradient-to-r from-indigo-500 via-purple-500 to-fuchsia-500" : "border border-white/10 bg-white/[0.04]"}`}>Start with {plan.name}</Link></Card>)}</section><Card className="mt-10 p-6"><h2 className="text-xl font-bold">Visibility products</h2><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-400">Boosts, bumps, and pins are transparent paid placements. Standard keeps organic ranking separate to protect user trust.</p></Card></Shell>;
}
