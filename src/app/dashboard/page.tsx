import { offers, paymentMethods, reviews, sellers, tools } from "@/lib/data";
import { Badge, Button, Card, Nav, SectionTitle, Shell, Stat } from "@/components/ui";
import { riskTone } from "@/lib/helpers";

const seller = sellers[0];
const sellerTools = tools.filter((tool) => tool.providerSlug === seller.slug);
const sellerOffers = offers.filter((offer) => offer.sellerSlug === seller.slug);

export default function DashboardPage(){return <Shell><Nav/><SectionTitle eyebrow="Seller dashboard" title="Manage products, offers and payments" text="One place for providers and resellers to manage listings, features, status, boosts, appeals, analytics and accepted payment methods."/><section className="mt-8 grid gap-4 md:grid-cols-4"><Stat label="Product slots" value={`${sellerTools.length}/10`} detail="Pro provider plan"/><Stat label="Reseller offers" value={sellerOffers.length} detail="Listed buy options"/><Stat label="Active boosts" value="2" detail="Sponsored visibility"/><Stat label="Trust score" value={`${seller.trustScore}/100`} detail={`Disputes ${seller.disputeRate}`}/></section><section className="mt-8 grid gap-6 xl:grid-cols-[1fr_360px]"><div className="space-y-6"><Card id="products" className="overflow-hidden"><div className="border-b border-white/10 p-5"><h2 className="text-2xl font-black">Products</h2><p className="mt-1 text-sm text-slate-400">Control status, architecture, features and public facts.</p></div><div className="overflow-x-auto"><table className="w-full min-w-[900px] text-left text-sm"><thead className="border-b border-white/10 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-4">Product</th><th className="px-5 py-4">Status</th><th className="px-5 py-4">Integrity</th><th className="px-5 py-4">Features</th><th className="px-5 py-4 text-right">Actions</th></tr></thead><tbody>{sellerTools.map((tool)=><tr key={tool.slug} className="border-b border-white/5 last:border-0"><td className="px-5 py-5"><div className="font-bold">{tool.name}</div><div className="text-xs text-slate-500">{tool.game} • {tool.architecture}</div></td><td className="px-5 py-5"><Badge tone={riskTone(tool.toolStatus) as any}>{tool.toolStatus}</Badge></td><td className="px-5 py-5"><div className="font-bold">{tool.integrityIndex}/100</div><div className="text-xs text-slate-500">{tool.verifiedReviews} verified reviews</div></td><td className="px-5 py-5"><div className="flex flex-wrap gap-1.5">{tool.features.slice(0,3).map((f)=><span key={f} className="rounded-md border border-white/10 bg-white/[0.04] px-2 py-1 text-xs text-slate-300">{f}</span>)}</div></td><td className="px-5 py-5 text-right"><button className="rounded-xl border border-white/10 bg-white/[0.04] px-4 py-2 text-sm font-semibold">Edit</button></td></tr>)}</tbody></table></div></Card><Card className="p-6"><h2 className="text-2xl font-black">Reseller Offers</h2><p className="mt-2 text-sm text-slate-400">List yourself as an official provider or verified reseller under a tool with payment methods, stock and delivery time.</p><div className="mt-5 space-y-3">{offers.slice(0,4).map((offer)=><div key={`${offer.sellerSlug}-${offer.toolSlug}`} className="rounded-2xl border border-white/10 bg-slate-950/40 p-4"><div className="flex flex-col justify-between gap-3 md:flex-row md:items-center"><div><div className="font-bold">{offer.seller}</div><div className="mt-1 text-xs text-slate-500">{offer.kind} • {offer.price} • {offer.delivery} • {offer.stock}</div></div><Badge tone={offer.kind==="Official Provider"?"green":"purple"}>{offer.kind}</Badge></div></div>)}</div></Card><Card className="p-6"><h2 className="text-2xl font-black">Reviews & appeals</h2><div className="mt-5 grid gap-3 md:grid-cols-2">{reviews.map((review)=><div key={review.title} className="rounded-2xl border border-white/10 bg-slate-950/40 p-4"><div className="font-bold">{review.title}</div><p className="mt-2 text-sm text-slate-400">{review.body}</p><div className="mt-3 flex gap-2"><Badge tone={review.verified?"green":"default"}>{review.verified?"Verified purchase":"Unverified"}</Badge><Badge>★ {review.rating}</Badge></div></div>)}</div></Card><Card className="p-6"><h2 className="text-2xl font-black">Analytics</h2><div className="mt-5 grid gap-3 md:grid-cols-4"><Stat label="Views" value="38.6K"/><Stat label="Outbound" value="2.1K"/><Stat label="Boost CTR" value="6.2%"/><Stat label="Payment clicks" value="842"/></div><div className="mt-6 h-44 rounded-2xl border border-white/10 bg-slate-950/40 p-4"><div className="flex h-full items-end gap-2">{[28,36,44,62,58,72,66,80,74,92,68,76].map((h,i)=><div key={i} className="flex-1 rounded-t-md bg-gradient-to-t from-purple-600/40 to-cyan-300/90" style={{height:`${h}%`}} />)}</div></div></Card></div><aside className="space-y-6"><Card className="p-5"><h3 className="text-xl font-black">Accepted payments</h3><p className="mt-2 text-sm text-slate-400">Select payment methods sellers can display publicly.</p><div className="mt-4 flex flex-wrap gap-2">{paymentMethods.slice(0,8).map((m)=><Badge key={m.id} tone={riskTone(m.risk) as any}>{m.name}</Badge>)}</div><Button href="/payments" variant="secondary" className="mt-5 w-full">Manage payment profile</Button></Card><Card className="p-5"><h3 className="text-xl font-black">Boost & bump center</h3><p className="mt-2 text-sm text-slate-400">Promote visibility without affecting organic rank.</p><div className="mt-4 space-y-3"><div className="rounded-xl border border-white/10 p-3"><div className="font-bold">Standard Boost</div><div className="text-xs text-slate-500">$49 / 7 days</div></div><div className="rounded-xl border border-white/10 p-3"><div className="font-bold">Pin Placement</div><div className="text-xs text-slate-500">$129 / 14 days</div></div></div></Card><Card className="p-5"><h3 className="text-xl font-black">Builder</h3><p className="mt-2 text-sm text-slate-400">Maintain technical facts, features, pricing, gallery and changelog.</p><div className="mt-4 grid gap-2">{["Hero", "Technical facts", "Features", "Payments", "FAQ", "Changelog"].map((x)=><div key={x} className="rounded-xl border border-white/10 bg-white/[0.03] p-3 text-sm">{x}</div>)}</div></Card></aside></section></Shell>}
