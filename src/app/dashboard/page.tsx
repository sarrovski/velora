import { Nav } from "@/components/nav";
import { Badge, Card, Metric, MiniStat, Pill, Shell, StatusBadge } from "@/components/ui";
import { plans, reviews, sellers, tools } from "@/lib/data";
import type { ReactNode } from "react";

const seller = sellers[0];
const sellerTools = tools.filter(t => t.sellerSlug === seller.slug);
const activeBoosts = sellerTools.filter(t => t.sponsored || t.pinned || t.bumped);
const sellerReviews = reviews.filter(r => sellerTools.some(t => t.slug === r.toolSlug));

export default function DashboardPage() {
  return <Shell><Nav /><section className="mt-12"><Badge>Seller area</Badge><h1 className="mt-4 text-5xl font-black">Seller Dashboard</h1><p className="mt-3 max-w-3xl text-slate-400">Manage your tools, features, product status, boosts, reviews, appeals, analytics, billing, and seller trust.</p></section><section className="mt-8 grid gap-4 md:grid-cols-4"><Metric label="Product slots" value={seller.productSlots} detail={`${seller.plan} plan`} /><Metric label="Active visibility" value={String(activeBoosts.length)} detail="Boosts / pins / bumps" /><Metric label="Trust score" value={`${seller.trustScore}/100`} detail={seller.status} /><Metric label="Reviews" value={String(sellerReviews.length)} detail="Including appeals" /></section><section className="mt-8 grid gap-6 xl:grid-cols-[1fr_360px]"><div className="space-y-6"><Panel title="Seller alerts"><div className="grid gap-3 md:grid-cols-3"><Alert title="Boost expiring" text="PhantomX sponsored slot ends soon." /><Alert title="Pending review" text="Shadow Overlay needs admin approval." /><Alert title="Appeal pending" text="One review appeal is awaiting admin decision." /></div></Panel><Panel title="Products"><div className="overflow-x-auto"><table className="w-full min-w-[980px] text-left text-sm"><thead className="border-b border-white/10 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-4 py-3">Product</th><th className="px-4 py-3">Platform</th><th className="px-4 py-3">Tool</th><th className="px-4 py-3">Features</th><th className="px-4 py-3">Stats</th><th className="px-4 py-3 text-right">Actions</th></tr></thead><tbody>{sellerTools.map(tool => <tr key={tool.slug} className="border-b border-white/5 last:border-0"><td className="px-4 py-4"><div className="font-semibold">{tool.name}</div><div className="text-xs text-slate-500">{tool.game} • {tool.category}</div></td><td className="px-4 py-4"><StatusBadge value={tool.status} /></td><td className="px-4 py-4"><StatusBadge value={tool.toolStatus} /></td><td className="px-4 py-4"><div className="flex flex-wrap gap-1.5">{tool.features.slice(0,3).map(f => <Pill key={f}>{f}</Pill>)}</div></td><td className="px-4 py-4"><div>★ {tool.rating || "—"}</div><div className="text-xs text-slate-500">{tool.clicks} clicks</div></td><td className="px-4 py-4 text-right"><button className="rounded-xl border border-white/10 bg-white/[0.04] px-4 py-2 text-xs font-semibold">Edit</button></td></tr>)}</tbody></table></div></Panel><Panel title="Product builder"><div className="grid gap-4 md:grid-cols-3">{["Hero", "Gallery", "Features", "Pricing", "FAQ", "Changelog"].map(section => <div key={section} className="rounded-2xl border border-white/10 bg-slate-950/40 p-4"><div className="font-semibold">{section}</div><div className="mt-1 text-xs text-slate-500">Editable block</div></div>)}</div></Panel><Panel title="Reviews & appeals"><div className="grid gap-3 md:grid-cols-2">{sellerReviews.map(r => <div key={r.title} className="rounded-2xl border border-white/10 bg-slate-950/40 p-4"><div className="flex justify-between gap-3"><div className="font-semibold">{r.title}</div><span className="text-amber-300">★ {r.rating}</span></div><p className="mt-2 text-sm text-slate-400">{r.body}</p><div className="mt-3 flex justify-between text-xs text-slate-500"><span>{r.user}</span><StatusBadge value={r.status} /></div></div>)}</div></Panel></div><aside className="space-y-6"><Panel title="Boost / bump center"><div className="space-y-3">{activeBoosts.map(t => <div key={t.slug} className="rounded-2xl border border-purple-400/20 bg-purple-500/10 p-4"><div className="font-semibold">{t.name}</div><div className="mt-1 text-xs text-slate-400">{t.sponsored ? "Sponsored" : t.pinned ? "Pinned" : "Bumped"} • Updated {t.lastUpdated}</div></div>)}</div><button className="mt-4 w-full rounded-xl bg-gradient-to-r from-indigo-500 via-purple-500 to-fuchsia-500 px-4 py-3 text-sm font-semibold">Create visibility campaign</button></Panel><Panel title="Analytics"><div className="grid gap-3"><MiniStat label="Views" value="64.8K" /><MiniStat label="Outbound clicks" value="4.1K" /><MiniStat label="CTR" value="6.3%" /><MiniStat label="Boost clicks" value="912" /></div></Panel><Panel title="Billing"><div className="space-y-3">{plans.map(p => <div key={p.name} className={`rounded-2xl border p-4 ${p.name === seller.plan ? "border-purple-400/30 bg-purple-500/10" : "border-white/10 bg-slate-950/40"}`}><div className="font-bold">{p.name}</div><div className="text-xs text-slate-500">{p.products} products • {p.boosts} boosts</div></div>)}</div></Panel><Panel title="Settings"><p className="text-sm text-slate-400">Seller status: <StatusBadge value={seller.status} /></p><p className="mt-3 text-sm text-slate-400">Public slug: /sellers/{seller.slug}</p></Panel></aside></section></Shell>;
}
function Panel({ title, children }: { title: string; children: ReactNode }) { return <Card className="overflow-hidden"><div className="border-b border-white/10 p-5"><h2 className="text-xl font-bold">{title}</h2></div><div className="p-5">{children}</div></Card>; }
function Alert({ title, text }: { title: string; text: string }) { return <div className="rounded-2xl border border-white/10 bg-slate-950/40 p-4"><div className="font-semibold">{title}</div><p className="mt-1 text-sm text-slate-400">{text}</p></div>; }
