"use client";

import { useMemo, useState } from "react";
import { PageShell, PublicNav } from "@/components/layout/Nav";
import { Badge } from "@/components/ui/Badge";
import { Button } from "@/components/ui/Button";
import { Card } from "@/components/ui/Card";
import { StatusBadge } from "@/components/ui/StatusBadge";
import { analytics, boostPackages, reviews, sellers, tools } from "@/lib/data";

const tabs = ["Overview", "Products", "Builder", "Boosts", "Reviews", "Analytics", "Billing", "Settings"];

export default function DashboardPage() {
  const [tab, setTab] = useState("Overview");
  const seller = sellers[0];
  const sellerTools = tools.filter((tool) => tool.sellerSlug === seller.slug);
  const pendingAppeals = reviews.filter((review) => review.appealStatus === "Pending Appeal").length;
  const activeBoosts = sellerTools.filter((tool) => tool.sponsored).length;
  const avgRating = useMemo(() => (sellerTools.filter(t => t.rating).reduce((sum, t) => sum + t.rating, 0) / sellerTools.filter(t => t.rating).length).toFixed(1), [sellerTools]);

  return (
    <PageShell>
      <PublicNav />
      <main className="mx-auto max-w-7xl px-6 py-8">
        <div className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><Badge tone="purple">Seller Area</Badge><h1 className="mt-4 text-5xl font-black tracking-tight">Seller Dashboard</h1><p className="mt-3 max-w-2xl text-slate-400">Manage products, features, status, boosts, reviews, analytics, billing, and settings.</p></div><Button href="#products">+ Add Product</Button></div>
        <div className="mt-8 flex gap-2 overflow-x-auto pb-2">{tabs.map((item) => <button key={item} onClick={() => setTab(item)} className={`rounded-xl px-4 py-2 text-sm font-semibold ${tab === item ? "bg-white text-slate-950" : "border border-white/10 bg-white/[0.04] text-slate-300"}`}>{item}</button>)}</div>
        <section className="mt-6 grid gap-4 md:grid-cols-4"><Metric label="Product Slots" value={`${sellerTools.length}/${seller.productLimit}`} detail={`${seller.plan} plan`} /><Metric label="Active Boosts" value={String(activeBoosts)} detail="Sponsored visibility" /><Metric label="Clicks" value="2.1k" detail="+18.6%" /><Metric label="Avg Rating" value={avgRating} detail="410 reviews" /></section>
        <div className="mt-8">{tab === "Overview" && <Overview setTab={setTab} pendingAppeals={pendingAppeals} />}{tab === "Products" && <Products sellerTools={sellerTools} setTab={setTab} />}{tab === "Builder" && <Builder />}{tab === "Boosts" && <Boosts />}{tab === "Reviews" && <ReviewManager />}{tab === "Analytics" && <Analytics />}{tab === "Billing" && <Billing />}{tab === "Settings" && <Settings />}</div>
      </main>
    </PageShell>
  );
}

function Metric({ label, value, detail }: { label: string; value: string; detail: string }) { return <Card className="p-5"><p className="text-sm text-slate-400">{label}</p><p className="mt-3 text-3xl font-black">{value}</p><p className="mt-1 text-xs text-slate-500">{detail}</p></Card>; }
function Overview({ setTab, pendingAppeals }: { setTab: (tab: string) => void; pendingAppeals: number }) { return <div className="grid gap-6 xl:grid-cols-[1fr_360px]"><div className="space-y-6"><Card className="p-5"><h2 className="text-xl font-bold">Seller Alerts</h2><div className="mt-4 grid gap-3 md:grid-cols-3"><Alert title="Boost ending soon" text="PhantomX boost ends soon." onClick={() => setTab("Boosts")} /><Alert title="Review appeal" text={`${pendingAppeals} appeal needs admin review.`} onClick={() => setTab("Reviews")} /><Alert title="Pending product" text="Shadow Overlay is awaiting approval." onClick={() => setTab("Products")} /></div></Card><Analytics compact /></div><div className="space-y-6"><Card className="p-5"><Badge tone="amber">Boost Center</Badge><h3 className="mt-3 text-2xl font-black">1 active boost</h3><p className="mt-2 text-sm leading-6 text-slate-400">Boosts create visibility but never affect organic ranking.</p><Button className="mt-4 w-full" href="#" variant="secondary">Manage Boost</Button></Card><Card className="p-5"><Badge tone="green">Recommended Action</Badge><p className="mt-3 text-sm leading-6 text-slate-400">Ask recent buyers for reviews after major updates to protect ranking.</p></Card></div></div>; }
function Alert({ title, text, onClick }: { title: string; text: string; onClick: () => void }) { return <button onClick={onClick} className="rounded-2xl border border-white/10 bg-slate-950/50 p-4 text-left transition hover:border-purple-400/40"><div className="font-semibold">{title}</div><p className="mt-1 text-sm text-slate-400">{text}</p></button>; }
function Products({ sellerTools, setTab }: { sellerTools: typeof tools; setTab: (tab: string) => void }) { return <Card id="products" className="overflow-hidden"><div className="border-b border-white/10 p-5"><h2 className="text-xl font-bold">Products</h2><p className="mt-1 text-sm text-slate-400">Control product status, features, boosts, and visibility.</p></div><div className="overflow-x-auto"><table className="w-full min-w-[980px] text-left text-sm"><thead className="border-b border-white/10 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-4">Product</th><th className="px-5 py-4">Platform</th><th className="px-5 py-4">Tool Status</th><th className="px-5 py-4">Features</th><th className="px-5 py-4">Stats</th><th className="px-5 py-4 text-right">Actions</th></tr></thead><tbody>{sellerTools.map((tool) => <tr key={tool.slug} className="border-b border-white/5 last:border-0"><td className="px-5 py-5"><div className="font-semibold">{tool.name}</div><div className="text-xs text-slate-500">{tool.game} • {tool.category}</div></td><td className="px-5 py-5"><StatusBadge value={tool.publicationStatus} /></td><td className="px-5 py-5"><StatusBadge value={tool.toolStatus} /></td><td className="px-5 py-5"><div className="flex max-w-[300px] flex-wrap gap-1.5">{tool.features.slice(0,3).map((feature) => <span key={feature} className="rounded-md border border-white/10 bg-white/[0.04] px-2 py-1 text-xs text-slate-300">{feature}</span>)}</div></td><td className="px-5 py-5"><div>★ {tool.rating || "—"}</div><div className="text-xs text-slate-500">{tool.clicks} clicks</div></td><td className="px-5 py-5"><div className="flex justify-end gap-2"><button onClick={() => setTab("Boosts")} className="rounded-xl border border-white/10 bg-white/[0.04] px-4 py-2 text-sm font-semibold">Boost</button><button onClick={() => setTab("Builder")} className="rounded-xl border border-white/10 bg-white/[0.04] px-4 py-2 text-sm font-semibold">Edit</button></div></td></tr>)}</tbody></table></div></Card>; }
function Builder() { return <Card className="p-6"><Badge tone="purple">Page Builder</Badge><h2 className="mt-4 text-3xl font-black">Edit product pages</h2><p className="mt-2 text-slate-400">Sellers can manage hero content, screenshots, features, pricing, FAQ, changelog, and public tool status.</p><div className="mt-6 grid gap-3 md:grid-cols-3">{["Hero", "Gallery", "Features", "Pricing", "FAQ", "Changelog", "Tool Status", "SEO", "Submit Review"].map((item) => <div key={item} className="rounded-2xl border border-white/10 bg-slate-950/50 p-4"><div className="font-semibold">{item}</div><div className="mt-1 text-xs text-slate-500">Editable block</div></div>)}</div></Card>; }
function Boosts() { return <div className="grid gap-6 lg:grid-cols-[1fr_360px]"><Card className="p-6"><Badge tone="amber">Paid boosts</Badge><h2 className="mt-4 text-3xl font-black">Boost published products</h2><p className="mt-2 text-slate-400">Only published products can be boosted. Sponsored placements never affect organic ranking.</p><div className="mt-6 grid gap-4 md:grid-cols-3">{boostPackages.map((pack) => <div key={pack.name} className="rounded-2xl border border-white/10 bg-slate-950/50 p-5"><div className="font-bold">{pack.name}</div><div className="mt-3 text-3xl font-black">{pack.price}</div><p className="mt-2 text-sm text-slate-400">{pack.duration} • {pack.placement}</p></div>)}</div></Card><Card className="p-6"><h3 className="text-xl font-bold">Active campaign</h3><p className="mt-3 text-sm text-slate-400">PhantomX Tracker • Standard Boost</p><div className="mt-5 grid grid-cols-2 gap-3"><Metric label="Spend" value="$49" detail="7 days" /><Metric label="Clicks" value="912" detail="Boost clicks" /></div></Card></div>; }
function ReviewManager() { return <Card className="p-6"><Badge tone="green">Review Management</Badge><h2 className="mt-4 text-3xl font-black">Reviews & appeals</h2><p className="mt-2 text-slate-400">Sellers cannot edit or delete reviews. They can appeal, and admins decide.</p><div className="mt-6 grid gap-3 md:grid-cols-2">{reviews.map((review) => <div key={review.id} className="rounded-2xl border border-white/10 bg-slate-950/50 p-4"><div className="flex items-center justify-between gap-3"><div className="font-semibold">{review.title}</div><div className="text-amber-300">★ {review.rating}</div></div><p className="mt-2 text-sm leading-6 text-slate-400">{review.body}</p><div className="mt-3 flex items-center justify-between"><span className="text-xs text-slate-500">By {review.user}</span><StatusBadge value={review.appealStatus} /></div></div>)}</div></Card>; }
function Analytics({ compact = false }: { compact?: boolean }) { const max = Math.max(...analytics.timeline.map(d => d.views)); return <div className="space-y-6"><Card className="p-6"><Badge tone="cyan">Analytics</Badge><h2 className="mt-4 text-3xl font-black">Performance overview</h2><div className="mt-6 grid gap-3 md:grid-cols-4"><Metric label="Product views" value={String(analytics.productViews)} detail="Last 30 days" /><Metric label="Outbound clicks" value={String(analytics.outboundClicks)} detail="Website visits" /><Metric label="CTR" value={analytics.ctr} detail="Conversion" /><Metric label="Rank movement" value={analytics.rankMovement} detail="Organic" /></div></Card>{!compact && <div className="grid gap-6 lg:grid-cols-2"><Card className="p-6"><h3 className="text-xl font-bold">Traffic trend</h3><div className="mt-6 space-y-4">{analytics.timeline.map((d) => <div key={d.day} className="grid grid-cols-[44px_1fr_80px] items-center gap-3 text-sm"><div className="text-slate-500">{d.day}</div><div className="h-3 rounded-full bg-white/10"><div className="h-full rounded-full bg-purple-400" style={{ width: `${Math.round((d.views / max) * 100)}%` }} /></div><div className="text-right text-slate-400">{d.clicks}</div></div>)}</div></Card><Card className="p-6"><h3 className="text-xl font-bold">Traffic sources</h3><div className="mt-6 space-y-4">{analytics.sources.map((s) => <div key={s.name}><div className="flex justify-between text-sm"><span>{s.name}</span><span>{s.share}%</span></div><div className="mt-2 h-2 rounded-full bg-white/10"><div className="h-full rounded-full bg-cyan-300" style={{ width: `${s.share}%` }} /></div></div>)}</div></Card></div>}</div>; }
function Billing() { return <div className="grid gap-5 md:grid-cols-3">{["Starter", "Pro", "Big Seller"].map((plan) => <Card key={plan} className={`p-6 ${plan === "Pro" ? "border-purple-400/40 bg-purple-500/10" : ""}`}><h3 className="text-2xl font-black">{plan}</h3><p className="mt-3 text-sm text-slate-400">Product limits, boost limits, analytics level, and appeals access.</p><Button className="mt-6 w-full" variant={plan === "Pro" ? "secondary" : "primary"}>{plan === "Pro" ? "Current Plan" : "Switch Plan"}</Button></Card>)}</div>; }
function Settings() { return <Card className="p-6"><Badge>Settings</Badge><h2 className="mt-4 text-3xl font-black">Seller settings</h2><div className="mt-6 grid gap-4 md:grid-cols-2">{[["Seller name", "DevStudio"], ["Slug", "devstudio"], ["Trust score", "96/100"], ["Verified", "Yes"]].map(([label,value]) => <label key={label}><div className="mb-2 text-sm text-slate-400">{label}</div><input readOnly value={value} className="w-full rounded-xl border border-white/10 bg-slate-950/70 px-4 py-3 text-sm text-white outline-none" /></label>)}</div></Card>; }
